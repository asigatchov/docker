<h1>Статьи</h1>

Статей в блоге: <%= Article.public_count %> 

<ul>
  <% @articles.each do |article| %>
    <% unless article.archived? %>
    <li>
        <%= link_to article.title, article %>
  </li>
    <% end %>
  <% end %>
</ul>

<%= link_to "Создать статью", new_article_path %>
